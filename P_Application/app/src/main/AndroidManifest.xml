<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 음성 인식(STT)용 마이크 권한 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- http 통신 허용 (로컬 테스트용) -->
    <application
        android:name=".ParkingApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Pproject">
        <meta-data
            android:name="com.naver.maps.map.NCP_KEY_ID"
            android:value="${naverMapClientId}" />
        <activity android:name=".CampusMapActivity"/>
        <activity android:name=".HomeActivity"/>
        <activity android:name=".VoiceActivity"/>
        <activity android:name=".SlotGridActivity"/>
        <activity android:name=".NotificationsActivity"/>
        <activity android:name=".RegisterActivity"/>
        <activity android:name=".ParkingMapActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
