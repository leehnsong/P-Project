# 말해주차 FastAPI(YOLO) 서버 의존성
# 설치:  pip install -r requirements.txt
# (가상환경 권장:  python3 -m venv venv && source venv/bin/activate)

fastapi==0.136.3
uvicorn==0.49.0
ultralytics==8.4.66      # YOLO 추론 (torch/torchvision 자동 설치)
opencv-python==4.13.0.92  # 영상 디코딩/프레임 처리
numpy==2.4.6
